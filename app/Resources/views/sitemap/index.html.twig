{% extends '::layout.html.twig' %}

{% block content %}
    <ul class="cmf-sitemap">
        {% for url in urls %}
            <li>
                <a href="{{ url.location }}" title="{{ url.label }}">{{ url.label }}</a>
            </li>
        {% endfor %}
    </ul>
{% endblock %}
